{
  "name": "my-lambda-app",
  "version": "1.0.0",
  "description": "AWS Lambda application",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  },
  "devDependencies": {
    "jest": "^27.2.0",
    "serverless": "^2.66.2"
  }
}